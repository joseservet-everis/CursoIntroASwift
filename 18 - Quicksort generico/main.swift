// Ejemplo: QuickSort genérico
//
// En un ejemplo anterior, hemos implementado el algoritmo de ordenación QuickSort, pero solamente para arrays de enteros. Vamos a implementar el algoritmo quicksort como una extensión para cualquier tipo que conforme al protocolo Comparable